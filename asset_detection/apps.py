from django.apps import AppConfig


class AssetDetectionConfig(AppConfig):
    """Configuration for the Asset Detection app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'property_hero.asset_detection'
    verbose_name = 'Asset Detection'
